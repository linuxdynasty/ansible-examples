---
- include: sai-go-common.yml

- block:
  - set_fact:
      needed_container_state: running
      logspout_needed: no
    
  - include: "actions/initialize_tables.yml"

  - set_fact:
      needed_container_state: exited
    
  - include: "actions/initialize_tables.yml"

  when: initialize is defined
